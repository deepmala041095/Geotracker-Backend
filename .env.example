# PostgreSQL database URL (Render)
# Ensure sslmode=require for managed providers like Render
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public&sslmode=require"

# Server
PORT=3000
